/**
 * Read makomi.json and package.json
 * Generate app.js and the create directories if not present
 * @param makomiFile
 * @param packageFile
 * @param outputDir
 * @param cb
 */
exports.initialize = function(makomiFile,packageFile,outputDir,cb) {

  // require all dependencies

  // set up ports, view engine, etc. These are all static for a given engine.

  // load config from a non-checked-in source

  // setup any middleware requested by makomi.json, in order specified
    // actual middleware function

  //

}